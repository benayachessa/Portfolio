---
// src/components/Profile.astro
const skills = [
  "Astro", "React", "Tailwind CSS", "Firebase", "TypeScript", "Node.js"
];
---

<section id="profil" class="relative py-20 px-6 overflow-hidden">
  
  <div class="max-w-6xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
    
    <div class="relative order-2 md:order-1 flex justify-center">
      <div class="absolute inset-0 bg-gradient-to-tr from-blue-500/20 to-purple-500/20 rounded-full blur-3xl -z-10"></div>
      
      <div class="relative w-80 h-80 md:w-[500px] md:h-[500px] rounded-2xl overflow-hidden border-2 border-white/10 rotate-3 hover:rotate-0 transition-all duration-500 shadow-2xl">
        <img 
          src="/src/assets/me.png" 
          alt="Foto Profil" 
          class="w-full h-full object-cover grayscale hover:grayscale-0 transition-all duration-500"
        />
      </div>
    </div>

    <div class="order-1 md:order-2 space-y-6">
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-blue-500/30 bg-blue-500/10 text-[var(--sec)] shiny-sec font-medium tracking-wider uppercase">
        <span class="w-2 h-2 rounded-full bg-[var(--sec)] animate-pulse"></span>
        Tentang Saya
      </div>

      <h2 class="text-3xl md:text-5xl font-bold text-white leading-tight">
        Membangun Aplikasi digital dengan <span class="text-[var(--sec)] shiny-sec">kode & kreativitas.</span>
      </h2>

      <p class="text-neutral-400 text-lg leading-relaxed">
        Halo! Saya adalah seorang developer yang fokus pada pengembangan web modern. 
        Saya senang menggabungkan desain dengan performa yang cepat menggunakan teknologi terkini.
      </p>

      <div class="space-y-3 pt-4">
        <h3 class="text-sm font-semibold text-white">Teknologi yang saya gunakan:</h3>
        <div class="flex flex-wrap gap-2">
          {skills.map((skill) => (
            <span class="px-4 py-2 text-sm bg-neutral-800/50 border border-white/5 text-neutral-300 rounded-lg hover:border-blue-500/50 hover:text-white transition-colors cursor-default">
              {skill}
            </span>
          ))}
        </div>
      </div>
    </div>

  </div>
</section>